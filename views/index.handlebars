<style media="screen">
body{
  background-color: orange;
}
#left{
  float: right;
}
  #textbox{
    margin-top: 95px;
  }
  #heading{
    text-align: center;
  }
</style>
<div class="container">
  <div class="row" id="heading">
    <div class="col-2"></div>
    <div class="col-8">
      <h1>
        Burger Eater!
      </h1>
     
      <h3>
        Program that utilizes Express, Handle-Bars, mySql, and jQuery.
      </h3>
    </div>
    <div class="col-2"></div>
  </div>
  <div class="row">
    <div class="col-6">
      <ul id="left">
      {{#each burgers}}
        {{#unless devoured}}
          <li>
            {{burger_name}}
          </li>
          <button>
            Devour it
          </button>
          <br />
          <br />
        {{/unless}}
      {{/each}}
    </ul>
    </div>
   
    <div class="col-6"> <ul id="right">
      {{#each burgers}}
        {{#if devoured}}
          <li>
            {{burger_name}}
          </li>
        {{/if}}
      {{/each}}
    </ul></div>
  </div>
  <div class="row">
     <div class="col-4">

     </div>
     <div class="col-4"><div class="input-group input-group-lg">
      <div class="input-group-prepend">
       <button type="button" class="btn btn-danger" id="addBurger">Add Burger</button>
      </div>
     <input type="text" >
       <div class="col-4"></div>
    </div>
  </div>
</div>